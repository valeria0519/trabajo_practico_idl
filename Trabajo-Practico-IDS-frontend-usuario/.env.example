# Copiar este archivo como ".env" en la raiz del proyecto y completar con
# valores propios. NUNCA subir el ".env" real (con contraseñas de verdad) a
# un repositorio: por eso .env esta listado en .gitignore.

DB_NAME=studyroom
DB_USER=studyroom_user
DB_PASSWORD=studyroom_password
